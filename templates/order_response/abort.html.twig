{% extends 'base.html.twig' %}

{% block header %}
    {# Intentionally left empty to not include the header #}
{% endblock %}

{% block title %}{% endblock %}

{% block body %}
    <div class="bg-gray-50">
        <div class="mx-auto max-w-2xl pt-16 sm:px-6 sm:py-24 lg:max-w-7xl lg:px-8">
                <div class="px-4 py-6 sm:px-6 lg:px-8">
                    <h2 class="text-xl font-bold tracking-tight text-red-600">Payment Aborted</h2>
                    <p class="mt-4 text-lg text-gray-600">Unfortunately, your payment did not go through. Please try again or contact support for assistance.</p>
                    <a href="{{ path('app_billing') }}" class="mt-6 inline-block text-sm font-medium text-indigo-600 hover:text-indigo-500">
                        Return to Shopping Cart
                        <span aria-hidden="true"> &rarr;</span>
                    </a>
                </div>
                <!-- Existing template content for successful order -->
                <div class="space-y-2 px-4 sm:flex sm:items-baseline sm:justify-between sm:space-y-0 sm:px-0">
                    <!-- Content omitted for brevity -->
                </div>

                <!-- Products -->
                <div class="mt-6">
                    <!-- Content omitted for brevity -->
                </div>

                <!-- Billing -->
                <div class="mt-16">
                    <!-- Content omitted for brevity -->
                </div>
        </div>
    </div>
{% endblock %}
